%% generate_Feat.m
% Find .wav files, import and compute spectral flux, then save all in a .mat
% file

cd('~/Desktop/Following/ANALYSIS/')
datadir = '../DATA/piece ';

Feat = struct();
for p = 1:n



for trial = 1:9
    StimFeat(trial).stimname = stimfiles{trial};
end

